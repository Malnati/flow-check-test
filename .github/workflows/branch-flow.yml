# .github/workflows/branch-flow.yml
name: branch-flow

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: write
  # 'issues: write' não é mais necessário se não usamos mais o script de delete manual,
  # mas pull-requests: write cobre comentários em PRs.

jobs:
  flow-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # 1. Análise
      - name: Guard
        id: flow
        uses: Malnati/branch-flow-guard@v1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          output_file: "flow-compliance.json"

      # 2. Carregar Dados
      - name: Load Data
        id: data
        run: |
          {
            echo "json_content<<EOF"
            cat flow-compliance.json
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      # 3. Postar/Atualizar Comentário (Sticky)
      # Removemos o passo 'Delete Previous Comments' pois a v6 cuida disso
      - name: Post Comment
        uses: Malnati/pr-comment@v6.1.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pr_number: ${{ github.event.pull_request.number }}
          template_path: .github/workflows/pr-comment-branch-flow.md
          
          # ID ÚNICO para garantir que sempre atualize a mesma mensagem
          message_id: "branch-flow-guard-status"
          
          header_title: "🛡️ Branch Flow Guard"
          header_subject: "Validacao de Fluxo"
          
          # Renderização da Tabela
          body_message: |
            <div align="center">
            
            | 🚦 Status | 🛫 Origem | 🛬 Destino |
            | :---: | :---: | :---: |
            | **${{ fromJson(steps.data.outputs.json_content).ui.message_md }}** | `${{ fromJson(steps.data.outputs.json_content).context.head_branch }}` | `${{ fromJson(steps.data.outputs.json_content).context.base_branch }}` |
            
            </div>
          
          footer_result: >-
             ${{ !fromJson(steps.data.outputs.json_content).compliance.allowed && 
             format('⛔ Bloqueado (Código: {0})', fromJson(steps.data.outputs.json_content).compliance.violation_code) || 
             '✅ Autorizado' }}
          
          footer_advise: >-
             ${{ !fromJson(steps.data.outputs.json_content).compliance.allowed && 
             'Feche esta PR e alinhe as branches corretamente.' || 
             'Pode prosseguir com o Code Review.' }}

      # 4. Bloqueio Hard
      - name: Block PR if Violation
        if: ${{ !fromJson(steps.data.outputs.json_content).compliance.allowed }}
        run: |
          echo "::error title=Branch Flow Violation::O fluxo de branches está incorreto. Merge bloqueado."
          exit 1
